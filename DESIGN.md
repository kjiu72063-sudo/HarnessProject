# DESIGN.md — Harness Platform 设计思考

## 气质与意象

**关键词**: 精密、透明、可控、工程级

**具象场景**: 走进一间玻璃墙的工程实验室。白色操作台上排列着精密仪器，每台仪器的状态灯清晰可见——琥珀色表示运转中，绿色表示通过，红色表示异常。墙上大屏实时显示着流水线状态，数据在深色背景上以等宽字体流动。空气中有金属和冷空气的味道。一切复杂但不混乱，因为每个环节都有它的位置。

这不是一个"科技感酷炫"的产品，而是一个"工程级可靠"的产品。用户是工程师，他们需要的是掌控感——看到 Agent 在干什么、约束在哪里生效、质量门是否通过。

## 视觉策略

- **摄影/图像方向**: 不使用装饰性图片。界面本身就是视觉主体——数据面板、流程图、代码片段、状态指示器。
- **图形语言**: 几何精确。直角为主，圆角仅用于交互元素（按钮、卡片）。流程图使用锐利的折线箭头。状态用色块+图标双重编码（色盲安全）。

## 配色方案

| 色彩 | 用途 | 意象来源 |
|------|------|---------|
| `#0F1115` 深炭灰 | 主背景 | 实验室深色操作台面 |
| `#1A1D24` 石墨灰 | 面板/卡片背景 | 仪器外壳的亚光金属 |
| `#FFFFFF` 纯白 | 主文字 | 玻璃墙透出的光 |
| `#F59E0B` 琥珀 | 运转中/进行中 | 仪器状态灯——琥珀色 |
| `#10B981` 翡翠绿 | 通过/成功 | 仪器状态灯——绿色 |
| `#EF4444` 警示红 | 失败/错误 | 仪器状态灯——红色 |
| `#6B7280` 雾灰 | 次要文字/边界 | 混凝土墙面的中性灰 |
| `#3B82F6` 工程蓝 | 链接/可交互 | 蓝图纸的蓝色——工程通信的信号色 |

## 字体排版

- **中文**: Noto Sans SC（Google Fonts），清晰中性
- **英文/代码**: JetBrains Mono（Google Fonts），等宽，用于日志、状态码、代码展示
- **标题**: Inter（Google Fonts），几何感强，工程级
- **排版节奏**: 紧凑。信息密度高但留白精准——每一段空白都有结构意义。行高 1.5，标题与正文间距 24px，段落间距 16px。

## 动效与交互

- **运动性格**: 精准、克制。不是弹跳，不是缓动飘入，而是像仪器面板切换——快速、确定性。
- **缓动曲线**: `ease-out` 为主，持续 150-200ms。状态切换用颜色过渡，不用位移。
- **悬停**: 微妙的背景色变化（5% 亮度差），不缩放、不阴影扩散。
- **流程进度条**: 阶段切换时用扫描线动画（从左到右一道光线扫过），暗示"正在处理"。
- **实时日志**: 等宽字体逐行追加，新行淡入（opacity 0→1, 100ms），不滚动跳动。

## 设计禁忌

- 不要使用蓝紫色渐变背景
- 不要使用毛玻璃（backdrop-blur）做大面积容器
- 不要使用 emoji 作为功能图标（用 Lucide 图标库）
- 不要使用弹性动画（bounce/elastic）
- 不要在深色背景上使用低对比度灰色文字（最低 #6B7280）
- 不要让流程图变成装饰画——每个节点必须可交互
- 不要使用卡片阴影模拟深度——用边框和背景色差
