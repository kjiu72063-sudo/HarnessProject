# 43 · F004 M1/M2 文档级修复（L3 coder 执行记录）

- **步骤名称**: F004 修复微任务 — M1 api-spec.md 字段补齐与枚举更正 + M2 convention-to-rule-mapping.md 语义补写
- **执行时间**: 2026-08-20T05:00Z（沙箱时钟）
- **执行角色**: L3 编码 Agent (coder, 修复会话)
- **前置条件**: Controller Spec `docs/handbook/controller-specs/f004-fix-m1-m2.md`（8 验收标准 + 范围外清单）；L3 审查 journal 40 结论 2 FAIL(M1+M2)；diff 锚点 1a361b5

## 一、验证环境表

| 项 | 值（实测） |
|---|---|
| Python | 3.12.3 |
| uv | 0.12.5 |
| git | HEAD 2608d93 |
| verify.sh | 14/14 PASS |
| 环境防护 | 会话 env 无 UV_DEFAULT_INDEX/UV_INDEX_URL 残留（P010 零残留）；纯文档任务未触发 uv sync（P009 未触发）；UV_FROZEN=1 前置 verify.sh |

## 二、执行内容

冷启动 7 步完成后，按 Controller Spec 执行两条纯文档修复：

### M1（journal 40 标准 10）: api-spec.md 修复

目标文件 `docs/reference/api-spec.md` 约束管理段 Constraint 字段声明行：

1. **补齐 `source_key` 与 `enforcer` 两个字段**：原字段列表缺此两字段（Pydantic ConstraintBase 含 source_key: str 与 enforcer: str，api-spec 声明必须镜像）
2. **更正 `enforcement` 枚举值**：`verify_gate|agent_hint` → `mechanized|manual_review`（与 Pydantic Enforcement 枚举 + 设计文档数据模型节一致）
3. **补字段语义注释**：`source_key`（条目唯一标识键）与 `enforcer`（执行器标识）各附一行内联说明

修改前: `{ id, project_id, source(agents_md|manual), rule_type, rule_no, title, detail, enabled, enforcement(verify_gate|agent_hint), gate_ids, created_at, updated_at }`
修改后: `{ id, project_id, source(agents_md|manual), source_key, rule_no, title, detail, rule_type, enforcer, enforcement(mechanized|manual_review), gate_ids, enabled, created_at, updated_at }`；附加 source_key 与 enforcer 语义说明

### M2（journal 40 标准 11）: convention-to-rule-mapping.md 修复

目标文件 `docs/conventions/convention-to-rule-mapping.md`，在 F004 设计裁决行后追加一行：

- **补 agents_md 条目 `enabled=false` 语义描述**，口径与裁决③一致: "enabled=false 仅影响阶段 4 注入（不下发编码 Agent），不影响 verify.sh 实际执行；「禁用即跳过闸门」方向性拒绝"
- 实现方式: "coding_agent 注入时 filter enabled=True；validation gates 不过滤 enabled"
- 状态: ⚠️ 人工审查（语义约束，非机械闸门强制）
- AGENTS.md 映射: #10（全闸门通过）与裁决③

## 三、修复对照表

| 修复项 | 依据 | 目标文件 | 改动量 | diff 语义 |
|---|---|---|---|---|
| M1-a: 补 source_key/enforcer | journal 40 标准 10 缺陷 1 + Pydantic ConstraintBase 实际字段 | api-spec.md | +2 字段 +2 语义注释 | 字段声明与实现对齐 |
| M1-b: 更正 enforcement 枚举 | journal 40 标准 10 缺陷 2 + Pydantic Enforcement 枚举 mechanized/manual_review | api-spec.md | 替换 1 处 | 枚举名与实现对齐 |
| M2: 补 enabled=false 语义 | journal 40 标准 11 + 裁决③ | convention-to-rule-mapping.md | +1 行 | 裁决语义落地 |

## 四、验收标准逐条核对

| # | 标准 | 结果 | 证据 |
|---|---|---|---|
| 1 | api-spec.md Constraint 字段声明含 source_key 与 enforcer，字段语义与 Pydantic schema 一致 | ✅ | 字段列表已含 source_key/enforcer；语义注释与 ConstraintBase.source_key/min_length=1,max_length=100 + ConstraintBase.enforcer/str="manual" 一致 |
| 2 | api-spec.md enforcement 枚举值恰为 mechanized/manual_review，全文件无 verify_gate/agent_hint 残留 | ✅ | grep 零命中 verify_gate/agent_hint；字段声明为 enforcement(mechanized|manual_review) |
| 3 | convention-to-rule-mapping.md 含 agents_md enabled=false 语义描述，口径与裁决③一致 | ✅ | 新增行含"仅影响阶段 4 注入""不影响 verify.sh 实际执行""禁用即跳过闸门方向性拒绝"，与 journal 38 裁决③逐句对齐 |
| 4 | 改动范围恰 2 目标文件 + journal 43 + progress 追加行 | ✅ | git diff --name-only 恰 2 目标文件；journal + progress 另行提交 |
| 5 | 零代码触碰: server/ 与 src/ 零变动 | ✅ | git diff --name-only grep (server|src)/ 零命中 |
| 6 | verify.sh 14/14 PASS + uv.lock 零漂移 | ✅ | 14 PASS / 0 FAIL；git diff -- uv.lock 空 |
| 7 | journal 43 写入 + progress.txt 追加恰 1 行 | ✅ | 本文件 + progress 末行追加 |
| 8 | 两目标文件修改后仍 ≤300 行 | ✅ | api-spec.md=40 行, convention-to-rule-mapping.md=72 行 |

## 五、自报歧义（不裁定，备审）

1. **M1 字段排序口径**: api-spec.md 原字段按 {id, project_id, source, rule_type, rule_no, ...} 排列，修复后改为与 Pydantic ConstraintBase 声明顺序一致（project_id → source → source_key → rule_no → title → detail → rule_type → enforcer → enforcement → gate_ids → enabled）。原排列顺序无文档规范约束，本次选择以实现（Pydantic schema）为锚对齐——如 test-reviewer 认为应保持原序仅插入两字段，可指出修正。
2. **M2 AGENTS.md 映射列值**: 新增行的 AGENTS.md 列填 "#10, 裁决③"——#10 对应"全闸门通过"规则（verify.sh 是 enabled=false 不影响的实际执行器），裁决③是语义来源而非 AGENTS.md 规则编号。混合编号与裁决引用是否合规备审。

## 六、P 编号命中

| P 编号 | 命中场景 |
|---|---|
| P009 | 未触发（纯文档，无 uv sync） |
| P010 | 防护执行：verify.sh 全程 UV_FROZEN=1 前置，uv.lock 零漂移确认 |
| P011 | 提交前双向核对（git status --short + git diff --cached --stat） |
